---
import Avatar from './Avatar.astro';

interface Props {
  username: string;
}

const { username } = Astro.props;
---

<div class="flex items-center space-x-3">
  <span class="text-sm font-medium text-starlight-text">{username}</span>
  <Avatar name={username} />
</div>